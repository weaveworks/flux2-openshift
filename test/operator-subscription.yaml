apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: flux-subscription
  namespace: operators # all namespaces
spec:
  channel: alpha
  name: flux
  startingCSV: flux.v0.2.2
  source: flux
  sourceNamespace: olm
