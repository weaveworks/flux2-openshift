apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: flux-subscription
  namespace: openshift-marketplace # all namespaces
spec:
  channel: alpha
  name: flux
  startingCSV: flux.v0.2.4
  source: custom
  sourceNamespace: openshift-marketplace
